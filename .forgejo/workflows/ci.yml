# .forgejo/workflows/ci.yml
name: CI/CD

on:
  push:
  pull_request:

jobs:
  # ── Tester (körs alltid vid push/PR) ───────────────────────────────────────
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Bun
        run: |
          curl -fsSL https://bun.sh/install | bash
          echo "$HOME/.bun/bin" >> "$GITHUB_PATH"

      - name: Skapa .env filer
        run: |
          echo "POSTGRES_USER=postgres" >> .env
          echo "POSTGRES_PASSWORD=ci_password" >> .env
          echo "POSTGRES_DB=toads_delight" >> .env
          echo "RABBITMQ_USER=guest" >> .env
          echo "RABBITMQ_PASSWORD=guest" >> .env

          echo "DATABASE_URL=postgresql://postgres:ci_password@postgres:5432/toads_delight" >> backend/.env
          echo "RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672" >> backend/.env

      - name: Starta systemet
        run: docker compose up -d --build

      - name: Vänta tills systemet är redo
        run: |
          for i in $(seq 1 30); do
            curl -sf http://localhost:8015/api/health && break
            sleep 5
          done

      - name: Installera backend-beroenden
        working-directory: backend
        run: bun install

      - name: Kör integrationstester
        working-directory: backend
        run: bun test src/tests/flow.test.ts

  # ── Deploy (bara på main, kräver att test passerar) ───────────────────────
  deploy:
    needs: test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SERVER_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p ${{ secrets.SERVER_PORT }} ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts

          ssh -i ~/.ssh/deploy_key \
              -p ${{ secrets.SERVER_PORT }} \
              ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} \
              "cd /opt/examination4 && \
               export \$(grep -E '^FORGEJO_USER|^FORGEJO_TOKEN|^FORGEJO_HOST|^FORGEJO_PORT' .env | xargs) && \
               git fetch http://\${FORGEJO_USER}:\${FORGEJO_TOKEN}@\${FORGEJO_HOST}:\${FORGEJO_PORT}/mos/examination4.git && \
               git reset --hard FETCH_HEAD && \
               docker compose down && \
               docker compose up -d --build && \
               docker image prune -f && \
               docker compose ps && \
               echo 'Deploy done! 🐸'"
